<Window x:Class="Asset_Management_Platform.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Asset_Management_Platform.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" 
        DataContext="{Binding Main, Source={StaticResource Locator}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <!--<ListView ItemsSource="{Binding StockList}" Grid.Column="2" Grid.Row="1" Margin="5">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Ticker}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Description}"/>
                        <TextBlock Grid.Column="2" Text="{Binding Price}"/>
                        <TextBlock Grid.Column="3" Text="{Binding MarketValue}"/>
                        <TextBlock Grid.Column="4" Text="{Binding Yield}"/>
                        <TextBlock Grid.Column="5" Text="{Binding PeRatio}"/>
                        <TextBlock Grid.Column="6" Text="{Binding Ask}"/>
                        <TextBlock Grid.Column="7" Text="{Binding AskSize}"/>
                        <TextBlock Grid.Column="8" Text="{Binding Bid}"/>
                        <TextBlock Grid.Column="9" Text="{Binding BidSize}"/>
                        <TextBlock Grid.Column="10" Text="{Binding Volume}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
        <DataGrid ItemsSource="{Binding StockList}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="5" AlternatingRowBackground="AliceBlue" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ticker"  Binding="{Binding Ticker}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="Market Value" Binding="{Binding MarketValue}"/>
                <DataGridTextColumn Header="Yield" Binding="{Binding Yield}"/>
                <DataGridTextColumn Header="P/E Ratio" Binding="{Binding PeRatio}"/>
                <DataGridTextColumn Header="Ask" Binding="{Binding Ask}"/>
                <DataGridTextColumn Header="Ask Size" Binding="{Binding AskSize}"/>
                <DataGridTextColumn Header="Bid" Binding="{Binding Bid}"/>
                <DataGridTextColumn Header="Bid Size" Binding="{Binding BidSize}"/>
                <DataGridTextColumn Header="Volume" Binding="{Binding Volume}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
